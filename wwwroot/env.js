(function (window) {
    window.__env = window.__env || {};
   // Your REST API urls
    window.__env.apiUrl  = 'https://localhost:7047';
   // Setting this to false will disable console output
   window.__env.enableGreet = true;
}(this));